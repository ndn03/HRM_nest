import { ResponseI18nInterceptor } from './response-i18n.interceptor';

describe('ResponseI18nInterceptor', () => {
  it('should be defined', () => {
    expect(new ResponseI18nInterceptor()).toBeDefined();
  });
});
