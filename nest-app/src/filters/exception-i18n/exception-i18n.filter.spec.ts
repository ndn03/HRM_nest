import { ExceptionI18nFilter } from './exception-i18n.filter';

describe('ExceptionI18nFilter', () => {
  it('should be defined', () => {
    expect(new ExceptionI18nFilter({} as any)).toBeDefined();
  });
});
